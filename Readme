Push new project to git 

check for email id
git config --global user.email 

Switch to your email id before pushing any code
git config --global user.email "email@example.com" 

cd <directory>
git touch .gitignore
git init
git add .
git commit -m "Initial commit"
git remote rm origin  
git remote add origin https://github.com/jainshe/SingJavaTest.git
git push -u origin master
**********************************************************************************
Future commits-

check for email id
git config --global user.email 

Switch to your email id before pushing any code
git config --global user.email "email@example.com" 

git add <whichever changes>
git commit -m "<changes name>"
git pull origin master
git push origin master
**********************************************************************************




